import React from 'react';
import { View, Text } from 'react-native';

export default function OfflineSeminar({ navigation }: { navigation: any }) {
    return (
        <View>
            <Text></Text>
        </View>
    );
}
